# 知进伴学 · 「越用越聪明」智能体路线图（Goal + Loop 锚点）

> **总目标（Goal）**：让用户**只用自然语言**就能调用整个智能体的全部能力——提问、配置偏好、查看/调整上下文、增删记忆、消灭错题、扩充知识库——并且**每一轮问答都在滚动更新对用户的理解**，形成一个越用越懂他的「学习数字人」，由此建立粘性。
>
> 本文件是自驱动 loop 的终点定义与验收标准。loop 每轮推进一个增量（设计→编码→测试→发布→验收），直到三期全部 ✅。

## 角色（多角色交叉验证，互为校验）
- **PM/产品**：定义需求与验收标准，砍掉模糊功能。
- **交互设计**：界面与流程，确保用户「看得懂、能配置」。
- **iOS 架构**：iPad+iPhone 复用 AppState/组件，最小风险落地。
- **后端架构**：契约、上下文暴露、自然语言意图分流。
- **QA/对抗评审**：找漏洞/风险/自相矛盾/过度设计，质量闸门。
- **集成验收**：模拟器+真机+后端冒烟，对照验收标准签收。

## 三期目标与验收标准

### 一期：上下文可观测 + 设置精简（投入产出最高）
- **上下文/我的档案面板**：每轮问答清晰列出喂给模型的东西（问题、图片、本轮对话、偏好、命中长期记忆[带分]、错题、知识库、策略），每项**可开关本轮**、附「为何被调用」。两端一等公民。
- **设置精简**：砍掉重叠的 学习模式/讲解深度/学习策略 → 收敛为「一句话辅导偏好（自然语言）+ 2 个明确开关」；**学科自动识别**（不预设）；**恢复提示词配置入口**，与偏好统一为「辅导设置」。
- **验收**：① 任一轮问答可打开面板看到全部上下文项并逐项开关，开关即时影响下一轮；② 设置页只剩"偏好(NL)+开关+提示词"，无模糊下拉；③ 学科由内容自动打标；④ 两端一致；⑤ 回归不破坏现有问答。

### 二期：自然语言配置（NL → 调用智能体全部能力）
- **意图分流**：用户一句话若是「配置指令」（改偏好/删错题/记知识点/记住某事/调上下文）而非提问 → 识别意图 → 调对应能力 → 回执确认。后端已具 memory/mistakes/knowledge 写接口，补「意图识别 + 安全改写 + 确认」层。
- **app 内"管家"**：在对话里直接说"以后先给提示""把这道错题删了""记住我数学薄弱""新增知识点…"即可生效并可撤销。
- **验收**：① 10 条典型自然语言配置指令正确分流并生效+回执+可撤销；② 误分流率低（疑似配置但其实是提问 → 安全回退为提问）；③ 两端可用。

### 三期：可见的滚动记忆「数字人」
- **每轮问答后异步更新**画像/偏好/错题模式/已掌握知识，并**显式展示**「这次我更了解你了：…」，用户可查看、可纠正。
- **我的学习档案**：用户共同拥有、可编辑的数字人档案。
- **验收**：① 每轮后产生可见的记忆增量；② 用户可一键纠正/撤销某条记忆；③ 档案随使用稳定演进、检索可解释；④ 不污染（回放历史/配置指令不应误写记忆）。

## 工作流（每个增量都由「子智能体多角色」开发 + 交叉验证）
我（主控）做编排与集成；每个角色起一个**子智能体**，互相交叉验证：
1. **产品**子代理：读路线图+代码现状 → 需求与验收标准。
2. **交互**子代理：界面与流程设计（两端）。
3. **研发**子代理：按规格写代码（后端 surgical + AppState 共享核心 + iPad/iPhone 各接 UI）。多块独立时用 worktree 隔离并行，避免冲突。
4. **测试**子代理：独立验证（编译矩阵 + XCUITest + 后端冒烟 + 截图），不复用研发的判断。
5. **评审/对抗**子代理：审研发的 diff 与测试结论，找漏洞/回归/过度设计（交叉验证质量闸门）。
6. **验收**子代理：对照本期验收标准逐条签收。
7. 我集成：合并产出、处理 Mac 同步/构建/发布(bump-and-publish)、做最终判断。
> 原则：角色分工 + 交叉验证（研发≠测试≠评审，互不背书）；我负责协调与不可逆动作。
> **完全自主授权（用户 2026-06-25 重申）**：三期连续做完，含发布到真实 TestFlight，全程零停顿。**所有决定（含产品/设计取舍）由我自行评估拍板，不等用户**；遇分叉就选最符合总目标(越用越聪明)且最小风险的方案并记录理由。唯一例外=同一步骤反复失败且自身无法恢复(如签名/凭证彻底失效)，那时才报错求助。每期完成追加进度+memory，三期全 ✅ 后给唯一一次总报告。

## 进度
- [x] 一期 · 上下文面板
- [x] 一期 · 设置精简
- [x] 一期 · 提示词入口
- [x] 二期 · NL 意图分流
- [x] 二期 · NL 配置生效+撤销
- [x] 三期 · 滚动记忆更新
- [x] 三期 · 可纠正数字人档案

## 增量 · 拍照分题浮层 + 梯形校正 + 逐题批改（2026-06-27，commit 64b2dea）
三期外新需求（用户）：拍照答题/作业批改时把当前画面的每道题分割，盖多个透明浮层，点选一题只分析那道；并对拍歪的原图做梯形校正。多角色子代理设计交叉验证后落地。
- [x] 端上梯形校正（VNDetectDocumentSegmentation/Rectangles 取四角 + CIPerspectiveCorrection，三段式兜底→原图）
- [x] 端上题目分割（VNRecognizeText 文字行聚类，按题号/垂直间隙成块，大题为一块）
- [x] 透明浮层选题层（两端共享 QuestionRegionOverlay，编号/单选高亮/≥44pt 命中；用原图⇄用矫正图切换；未识别兜底；整图问答回退）
- [x] 选中题→裁剪子图→复用 submitQuestion/focus（扩展 region/crop/question_index/question_text，后端 build_qa_prompt 选中题聚焦规则，向后兼容）
- [x] 作业批改逐题（后端 批改JSON 全题判定块 + parse_batch_grading 落库；展示用 strip_structured_json_for_display 防机读块回放）
- [x] 两端入口 + 设置开关（多题自动分割 / 自动梯形校正）；4/4 编译矩阵过；后端部署+冒烟过
- 备注：相机/校正真机流只能 TestFlight 真机验收（模拟器无相机，沿用既有 QA 边界）。批改JSON 为前瞻后端能力，暂无 iOS 消费方。本次连带把 commit 0012d86 的「错题JSON 抽取重构」从未部署 WIP 一并上线 prod（用户批准）。

## 增量2 · 拍照答题/批改大改造（2026-06-27 起，研究驱动，P0→P4）
用户实测反馈 v1「没做好」+ 两张截图(图糊/暗/低清，分割识别到「文字1处/矩形0个」)。多智能体调研(竞品交互+OCR模型+图像增强)结论：
- **根因=拍摄质量**，不是分割逻辑。后处理救不回没拍到的细节；严重模糊不可逆。**修拍摄端是最高杠杆**。
- **A vs B(让图变清晰)**：A=矫正+文档专用增强真实像素；B=大模型重生成数字版。**B 对批改是错的**(会幻觉/篡改学生真实作答、几何靠猜、丢证据)。**定论：整体走 A；B 仅限印刷题干可选重排版且强制对照原图，绝不用于学生作答/几何**。(用户仍想试 B→按此护栏做实验)
- **OCR：需要专用模型+大模型分工**(hybrid wins，2×4090 放得下)：服务端 dewarp+增强(DocRes) → 版面分题(PP-DocLayout/DocLayout-YOLO) → 印刷字 PP-OCRv5 + 公式 PP-FormulaNet 转写 → 喂 27B Qwen-VL 做几何理解/语义判分。几何无即插即用开源模型→交大模型。
- **交互对标**：小猿口算/作业帮=结果直接在原照片上叠 ✓/✗ 逐题，点错看讲解(消费已就位的后端 批改JSON)；取景框+边缘纠偏+CLAHE+全分辨率+清晰度门控+自动快门。分流：用户选「自动识别+一键纠偏」(intent_router+画面信号)。
- 用户决定：**按 P0→P4 全做，每阶段发版验收**。
- [x] **P0 拍摄质量·引擎**(commit 4b19cfe)：.photo 全分辨率预设；QA/分题/批改改走 photoOutput 真照片(不再用 720p 预览帧)；连续自动对焦+点按对焦+画质优先设置；分割为空按 blur/light 给重拍引导。**真机验收**(相机模拟器测不了)。
- [ ] P0 余项：取景框 + 自动快门(稳定+清晰+充满取景才拍) + 补光提示 UI。
- [ ] P1 服务端 dewarp+增强+分题(dell 2×4090 起 DocRes+PP-DocLayout 服务，新端点；替换端上分割)。
- [ ] P2 逐题批改 UI(照片叠 ✓/✗，消费 批改JSON，点错看讲解)。
- [ ] P3 混合识别(PP-OCRv5+PP-FormulaNet 转写喂 27B)。
- [ ] P4 NL 自动分流(扩 intent_router，实时对话自动进 答题/批改 + 一键纠偏)。

### GOAL + 全自动 loop（用户 2026-06-27 重申：设 goal + loop，把答题和批改全部搞完，最后统一真机测）
**总目标**：让「拍照答题」与「作业批改」真正可用、稳、对标作业帮/小猿口算——**先判断画面里有没有题目印刷体(书本/试卷/课本/平板屏幕)，有才识别**；拍清楚→分题→选题只答那道/逐题批改→照片上叠✓/✗→点错看讲解；自然语言在实时对话里自动进答题/批改模块(+一键纠偏)。
**完全自主授权**：P0→P4 + B 连续做完，含真机发布 TestFlight，全程零停顿自行拍板；唯一例外=同一步反复失败且自身无法恢复(如签名/凭证彻底失效)。每阶段完成追加进度+memory；全部 ✅ 后给唯一总报告，用户统一真机测。
**验收(逐条可测)**：① 没对准题目时不自动拍、提示「请对准书本/试卷/屏幕上的题目」；对准清晰题目才自动/手动拍。② 拍到的是全分辨率清晰图(非预览帧)，分题能框出≥70%的题。③ 选一题只分析那道；整页批改逐题✓/✗叠在原图上、点错题看讲解。④ 实时对话说「这题怎么做」「帮我批改」自动进对应模块，误判可一键切换。⑤ B 实验：仅印刷题干可重排版且强制对照原图，学生作答/几何绝不重生成。⑥ 两端一致、不破坏现有问答/观察/记忆。
**loop 纪律**：① Mac(100.64.0.6) 离线时先做 Mac 无关的(后端/设计/GOAL)，并定期重试 Mac 做 iOS 编译+发布。② dell 2×4090 显存被 27B 占满(~2GB 空闲/卡)时，P1 先用**现有 27B VLM** 做服务端「是否题目+分题 bbox」JSON 端点(GPU 轻)，专用 OCR(DocRes/PP-OCRv5/PP-FormulaNet)等显存腾出或上 CPU 再补。③ 累积到阶段里程碑再出一个 TestFlight 包(尊重用户「统一测」)。
- [x] P0+ 先判断有没有题目印刷体(study-material gating)：取景实时 materialScore(VNRecognizeText 数文本行+矩形)，<0.5 不自动快门、提示对准题目；分割为空按 textCount/blur/light 给精准重拍引导。**(已码，待 Mac 恢复编译验证)**
- [x] P1 服务端分题端点 `POST /api/sessions/{id}/segment-questions`(复用27B VLM)：已部署 prod + **用10张真实作业照片深度测试(test→fix→retest 子代理循环)**。
  - **关键发现(定架构)**：VLM 判 `is_study_material` + `question_text` + `has_student_answer` **可靠(真题8/8)**；但 **bbox 是按题序编造的等距网格、非真实定位(漂移10-20%)** → **像素级✓/✗叠标不能只靠 VLM**，要么用端上 Vision(QuestionSegmenter 文字行位置准、但糊图/非题失效)，要么专用版面模型(P1-proper,GPU 现满)。响应已加 `bbox_approx:true`。
  - 修过的 bug：①JSON 解析脆弱致~50%假阴性(数字后多引号/截断)→分层容错解析(已修，10张里8张正确检出)；②一题号一条/练习标题并入(real2 由误并3→正确5)；③模糊吐空文本框→过滤+`low_quality`标志；temperature 已=0。
  - **架构结论**：服务端 VLM = 「有没有题/题干文本/是否作答/逐题判分」理解层(已就位)；端上 Vision = 「题在图上哪(准 bbox)」定位层；P2 批改 = 端上 bbox 定位 + VLM/批改JSON 判分，二者融合。
- [x] **里程碑包 build 202606272314** 已发 TestFlight(VALID,4 testers)：P0 全分辨率捕获+取景+study-material gating。相机/取景真机待用户测。
- [x] **P2 后端整页批改端点 `POST /api/sessions/{id}/grade-page`**(复用 VLM+鲁棒解析)：逐题 verdict+学生作答转写+订正。**10 张真测+视觉评测**发现并修：
  - **转写准、口算/纯算术判分可靠**；但 **VLM 几何/读图题编造标准答案(同题三照片三值、与自己 correction 矛盾)→学生写对也误判错**，是模型硬伤，提示词治不住。
  - **修法=服务端可靠性闸门**：question_text 含读图/几何线索(如图/阴影/三角形+面积…)→强制 verdict「不确定」+清 correct_answer+`gradable=false`；纯算术保持判分。响应加 `reliable_subset_only/gradable`。实测 real5 全几何题已正确变「不确定」。**产品定调(对标小猿口算)：只判可靠子集(口算/算术)，几何题给「讲解」不硬判对错。**
- [x] **P2 iOS 作业批改逐题 ✓/✗ UI**(commit 163c234)：作业批改→取景拍清→端上 Vision segment(准 bbox)→调 grade-page 整页判分→按 index 对齐叠 ✓(对)/✗(错)/◐(部分)/?(不确定·几何待讲解)；`gradable=false` 只显「?·待讲解」绝不红叉；点题→GradeDetailSheet(判分+学生作答+订正/思路+错因+知识点+「讲解这道题」)。整图问答兜底保留。两端编译过。新文件 QuestionGradingOverlay.swift。
- [ ] P4 NL 自动分流(扩 intent_router：实时对话说「这题怎么做/帮我批改」自动进答题/批改+一键纠偏)。
- [ ] B 印刷题干重排版(仅印刷题干、强制对照原图)。

## 约束
- 两端共享核心（AppState/AuthSession/模型/API），UI 各接一次（见 docs/MULTI_CLIENT_GUIDE.md）。
- 后端 surgical 部署，data/ 勿覆盖。
- 真机相机流不可在模拟器 E2E（见 docs/QA_TEST_REPORT.md）。
- **全自动**：设计/编码/测试/发布全程自动，三期连续做完，不在每期停（用户已授权）。
