{
  "summary": "多角色设计知进伴学二期(自然语言配置:意图分流→改偏好/记忆/错题/知识+确认/撤销)并对抗评审产出实现规格",
  "agentCount": 5,
  "logs": [],
  "result": "All facts confirmed. I have what I need to write the integrated, corrected spec. Producing the final spec.\n\n---\n\n# 二期实现规格：自然语言配置管家（intent_router）— 最小可发布版\n\n> 本规格已吸收评审的 9 项必改 + 7 项必砍，并校正了原稿中的事实错误（函数位置/不存在的函数/语音绕过/账户路由层）。所有行号以当前代码为准并已复核。**范围已收敛：首批仅 2 类意图，提案不落业务库、提案态用短 TTL 内存缓存，文本输入框触发，QA 主流程零改动。**\n\n---\n\n## 1. 范围与总则（已收敛）\n\n**首批意图仅 2 类**（评审砍清单 #3）：\n- `update_coach_preference` — 改辅导偏好（真源在 iOS `coachPreferenceText`）\n- `toggle_context` — 调上下文开关（真源在 iOS `contextInclusionSettings`，唯一写入口 `updateContextInclusion`）\n\n二者真源都在 iOS 本地、可逆=文本/布尔回写、不触碰任何后端业务库，是验证\"自然语言→预览→确认→撤销\"链路的最干净起点。\n\n**第二批（本规格给出接口预留，不实现）**：`remember_fact`、`delete_memory`（仅精确 id，无 match_text）、`delete_mistake`。**`add_knowledge` 延后**（后端无 knowledge 写入口，仅 reindex/search）。**`prompt_edit` 移出**（改系统级 prompt 与改辅导口气风险不对等，且原后端枚举里本就没有它）。\n\n**总则**：\n1. QA 主流程零改动 — `submitRecognizedQuestion`(ContentView.swift:8564) 及 `/qa` 一行不改。\n2. 确认前零业务写 — 首批两类本就不碰后端业务库；提案存内存短 TTL 缓存，不建持久化表（评审砍清单 #1/#2）。\n3. 不信任 LLM — 首批 `toggle_context` 的 `key` 必须命中白名单；不引用任何 id，无 id 幻觉面。\n4. 同步前置 hop 不收\"全局延迟税\" — 见 §5 本地关键词预筛先挡掉绝大多数纯提问。\n\n---\n\n## 2. 后端文件 / 端点 / 表\n\n### 2.1 文件\n- **新增** `/home/ubuntu/xue/xue/backend/app/intent_router.py` — 分类抽槽 + 提案缓存 + 3 端点 router。\n- **改** `/home/ubuntu/xue/xue/backend/app/main.py` — 仅 `app.include_router(intent_router.router)` 一行，并复用现有依赖把 `set_current_account` 注入（见 §2.4）。\n\n### 2.2 表 / 持久化（评审 #1：砍持久化表）\n**不建 `intent_proposals` 表。** 提案是\"确认即执行/秒级丢弃\"的短命对象，用进程内 TTL 缓存：\n\n```python\n# intent_router.py\n# key = proposal_id;  account 隔离靠 key 里编入 account 前缀 + 校验时比对\n_PROPOSALS: dict[str, dict] = {}          # {proposal_id: {account_id, payload, expires_at, status}}\n_PROPOSAL_TTL = 600                       # 10 min pending\n_UNDO_TTL = 24 * 3600                     # 24h 可撤窗口（confirmed 后保留）\n```\n\n每次 route/confirm/undo 入口惰性清掉过期项。**首批两类的 confirm 不写任何后端库**，故无需 undo_payload 落盘——undo 的旧值由前端在 confirm 响应里拿到的 `echo_state.before` 持有，或由缓存里的 proposal 携带。\n\n> 注：若第二批接入 memory/mistake（会写后端库），届时 undo 需要后端侧快照——那时再评估是否引入轻量持久化。首批不需要。\n\n### 2.3 端点（3 个，砍掉 GET /proposals — 评审 #2）\n\n```\nPOST /api/intent/route       Propose：分类+抽槽+生成预览，写内存 pending，不执行\nPOST /api/intent/confirm     执行/标记 confirmed，返回 echo_state 供 iOS 落本地真源\nPOST /api/intent/undo        回放逆值，返回 echo_state（旧值）\n```\n\n### 2.4 账户路由（评审必改 #6 — 越权防护写死）\n3 个端点必须复用 main.py 既有的 principal 依赖，使每次请求进入前已调用 `set_current_account(user[\"account_id\"])`（参照 main.py:561/7867 模式）。具体：intent_router 的每个 path operation 依赖同一个返回 `user` 的 `Depends(...)`，在该依赖体内 `set_current_account(user[\"account_id\"])`。**严禁裸端点**，否则 `connect()`(db.py:94) 落默认库 = 串号。首批两类虽不写后端库，但读取/校验/未来扩展都依赖该 contextvar，必须从一开始就挂上。\n\n---\n\n## 3. iOS 改动与组件\n\n### 3.1 新增文件（隔离对 11000 行 ContentView 的污染）\n- `/home/ubuntu/xue/xue/ios/Xue/App/Intent/IntentModels.swift` — `IntentProposal: Identifiable, Decodable`、`IntentDiffRow`、`enum IntentCategory { case preference, contextToggle }`、`enum IntentPhase { case proposed, applied, undone }`、`EchoState`。\n- `/home/ubuntu/xue/xue/ios/Xue/App/Intent/IntentProposalCard.swift` — `IntentProposalCard` 纯展示组件（两端共用，无 layout 假设）+ 分类 chip 颜色。\n- `/home/ubuntu/xue/xue/ios/Xue/App/Intent/IntentRouting.swift` — `extension AppState`：`maybeInterceptAsIntent(_:) async -> Bool`、`localPrefilterLooksLikeConfig(_:) -> Bool`、`confirmProposal()`、`undoLastIntent()`、`applyEchoState(_:)`、`dismissProposal()`。\n\n### 3.2 ContentView.swift 改动（逐处行级，唯一侵入点收口正确）\n\n**评审必改 #1 — 拦截点选对。** 事实核实：`submitTypedQuestion`(8124) 与文本 composer(731/1685) 走文本路径；语音/复习/quick-followup/双击共 5 个点(7674/8150/8181/8217/8521)**全部最终调 `submitRecognizedQuestion`(8564)**，不经 `submitTypedQuestion`。因此：\n\n- **首批明确范围：仅支持文本输入框触发配置。** 语音/复习/双击等路径**不做**意图拦截（产品上\"自然语言配置\"先只覆盖打字，符合最小发布）。这避免改动 `submitRecognizedQuestion`（=QA 主路径），实现\"QA 零改动\"的承诺在事实上成立。\n- 拦截只加在文本 composer 提交处。两个 composer(731/1685) 都调 `state.submitTypedQuestion(text)`，故只需改 `submitTypedQuestion`(8124) 一处：\n\n```swift\nfunc submitTypedQuestion(_ text: String) {\n    let question = text.trimmingCharacters(in: .whitespacesAndNewlines)\n    guard !question.isEmpty, !isSubmittingQuestion else { return }\n    // 新增：仅文本路径做配置探测，失败/超时/非配置一律降级为普通 QA\n    Task {\n        if await maybeInterceptAsIntent(question) { return }   // 命中配置 → 弹卡片，不进 QA\n        await MainActor.run { self.proceedTypedQuestion(question) }  // 原逻辑整体下移到 proceedTypedQuestion\n    }\n}\n```\n\n把 8124 现有函数体（8126 起 `suppressObservationDanmaku...` 到 8149 `Task{...}`）原样剪进新私有方法 `proceedTypedQuestion(_:)`，零行为改动。\n\n- **AppState 新增属性**（评审 §4，放 6207 附近）：\n```swift\n@Published var pendingIntentProposal: IntentProposal?\n@Published var lastAppliedProposal: IntentProposal?   // 持 echo_state.before 供就地撤销\n@Published var intentRouteInFlight = false\n```\n\n- **body 呈现**（239–268 附近，按 `@Environment(\\.horizontalSizeClass)` 分流，卡片本体同一份）：\n  - compact(iPhone)：`.sheet(item: $state.pendingIntentProposal) { IntentProposalCard(...) .presentationDetents([.medium]) }`\n  - regular(iPad)：`.overlay { if let p = state.pendingIntentProposal { dim + IntentProposalCard(...).frame(maxWidth: 420) } }`\n\n- **网络**：复用现有 `postJSON`/`getData`(11492/11511)，route 调用单独设短 timeout（见 §5）。\n\n### 3.3 同源回写（唯一真源不破）\n`applyEchoState(_:)` 按 category 调既有唯一写入口，**不新建并行状态**：\n\n| category | 写入口（既有真源） | 行号 |\n|---|---|---|\n| preference | 设 `coachPreferenceText` 后调 `coachPreferenceTextDidChange(_:)`（同步 UserDefaults key `xue.coachPreferenceText`） | 6207 / 3743 调用、:32 key |\n| contextToggle | 必须经 `updateContextInclusion(keyPath, to:)`，禁止直接赋 `contextInclusionSettings` | 6716 |\n\n---\n\n## 4. 意图 schema 与 Propose/Confirm/Undo 流程\n\n### 4.1 LLM 输出包络（首批 2 类，单次调用抽槽，砍三层管线 — 评审 #6）\n\n```jsonc\n{\n  \"intent_type\": \"update_coach_preference\" | \"toggle_context\" | \"qa\",\n  \"confidence\": 0.0,\n  \"slots\": { /* 见下 */ },\n  \"summary\": \"把辅导风格改成：先给提示再讲解\",\n  \"clarification\": null\n}\n```\n\n`slots`：\n- `update_coach_preference`: `{ \"mode\": \"replace\"|\"append\", \"text\": str }` — text 非空。\n- `toggle_context`: `{ \"key\": <白名单>, \"value\": bool }` — key ∈ `{visual, observation, history, mistakes, knowledge, memory, strategy, debug}`（对应 ContextInclusionSettings 字段，ContentView.swift:4197–4260），非法 key → clarify。\n\n### 4.2 Propose（POST /api/intent/route）\n\n请求：\n```jsonc\n{ \"text\": str, \"session_id\": str?,\n  \"app_state\": {                          // iOS 传入真源快照（preview.before 来源）\n    \"coach_preference_text\": str,\n    \"context_inclusion\": { \"<key>\": bool, ... } } }\n```\n\n后端流程：LLM 抽槽 → 校验（key 白名单 / text 非空 / `intent_type∈枚举`）→ 用 `app_state` 算 diff → 写内存 pending → 返回。**confidence<0.6 或槽位缺失 → 返回 needs_clarification，不出卡片。** `intent_type==\"qa\"` → 直接告诉 iOS 当普通提问处理。\n\n响应（config 命中）：\n```jsonc\n{\n  \"intent_kind\": \"config\",\n  \"proposal\": {\n    \"id\": \"ip_<account>_<uuid>\",\n    \"category\": \"preference\" | \"context_toggle\",\n    \"title\": \"调整上下文：关闭长期记忆\",\n    \"summary\": \"本轮起停用长期记忆上下文\",\n    \"diff\": [ { \"label\": \"长期记忆\", \"before\": \"开\", \"after\": \"关\" } ],\n    \"confirm_label\": \"确认修改\",\n    \"reversible\": true,\n    \"expires_at\": \"2026-06-25T..Z\"\n  }\n}\n```\n响应（非配置 / 不确定 / 误判逃生）：\n```jsonc\n{ \"intent_kind\": \"qa\" }                              // iOS 照常走 QA\n{ \"intent_kind\": \"config\", \"proposal\": null,\n  \"needs_clarification\": true, \"clarification\": \"你是想关掉记忆，还是在问记忆相关的问题？\" }\n```\n\n### 4.3 Confirm（POST /api/intent/confirm）\n请求：`{ \"proposal_id\": str }`\n流程：取缓存 pending → 校验未过期 + `account` 匹配（评审 #6 防串号）→ 标 `confirmed` 并保留到 `_UNDO_TTL` → 返回 `echo_state`（目标值 + before 旧值，供 iOS 落本地真源 + 持有撤销旧值）。**首批后端不写任何业务库**，confirm 仅状态流转 + 回吐目标值。\n```jsonc\n{ \"ok\": true, \"proposal_id\": \"ip_...\",\n  \"applied\": { \"category\": \"context_toggle\", \"summary\": \"已停用长期记忆\" },\n  \"undoable\": true,\n  \"echo_state\": {\n    \"category\": \"context_toggle\",\n    \"context_inclusion\": { \"memory\": false },         // iOS 经 updateContextInclusion 落地\n    \"before\": { \"context_inclusion\": { \"memory\": true } } } }\n```\niOS 收到 → `applyEchoState` 写真源 → 卡片转 `applied` 态 → 持 `lastAppliedProposal`(含 before)。\n\n### 4.4 Undo（POST /api/intent/undo）\n请求：`{ \"proposal_id\": str }`\n流程：校验 `status==confirmed` 且在 `_UNDO_TTL` 内 → **TOCTOU 二次校验（评审 #7）**：要求请求携带 `current_app_state`，后端比对\"当前值是否仍等于我当初改成的值\"，不一致则拒绝 undo（`409 state_drifted`），不盲目回写 → 否则返回 `echo_state`（before 旧值），标 `undone`（终态）。\n```jsonc\n{ \"ok\": true, \"proposal_id\": \"ip_...\", \"status\": \"undone\",\n  \"echo_state\": { \"category\": \"context_toggle\",\n                  \"context_inclusion\": { \"memory\": true } } }   // 旧值，iOS 写回\n```\n\n**跨端 undo 一致性（评审 #8）**：iOS 端 `applyEchoState` 必须在本地真源写入**成功后**才认为撤销完成；若写回失败（崩溃/切走），不更新 UI 终态，下次启动用本地真源为准（本地即真源，后端只是编排者，故本地写回失败=未撤销，不产生\"后端以为撤销了\"的永久漂移——因为首批后端不持有业务真源，仅持提案状态，状态机以\"是否收到 iOS 的应用确认\"为准）。为此 undo 响应后 iOS 可选回报 `POST /api/intent/confirm` 风格的轻量 ack（首批可省略，因本地为真源）。\n\n---\n\n## 5. 数据契约（汇总）\n\n| 端点 | 入参关键字段 | 出参关键字段 |\n|---|---|---|\n| route | `text`, `session_id?`, `app_state{coach_preference_text, context_inclusion{}}` | `intent_kind`, `proposal{id,category,title,summary,diff[],confirm_label,reversible,expires_at}` 或 `needs_clarification/clarification` |\n| confirm | `proposal_id` | `ok`, `applied{category,summary}`, `undoable`, `echo_state{category, coach_preference_text?/context_inclusion?, before{}}` |\n| undo | `proposal_id`, `current_app_state{...}`(TOCTOU) | `ok`, `status`, `echo_state{...旧值}` 或 `409 state_drifted` |\n\n`IntentProposal`(iOS Decodable) 字段与 route.proposal 一一对应。`category` 枚举首批仅 `preference`/`context_toggle`。\n\n**误分流降级税控制（评审 #2）**：iOS 在调 route 前先跑 `localPrefilterLooksLikeConfig(text)` —— 一组轻量本地关键词/句式（\"以后/记住/别再/打开/关闭/讲题/提示词/上下文/记忆…\" 等）。**不命中 → 完全不调 route，直接走 QA（零延迟税）**；命中 → 调 route（短 timeout 1.5s，超时/失败 → 降级为 QA）。这把\"每条提问串行等 LLM\"改成\"仅疑似配置才付往返\"。\n\n**误分类逃生（评审 #9）**：确认卡片底部固定一个\"我是在提问，不是配置 →继续提问\"按钮，点击即 `dismissProposal()` 并改走 `proceedTypedQuestion(原文)`，把误判事件本地计数上报（route 入参可带 `prefilter_hit` 便于后续校准 0.6 阈值）。\n\n---\n\n## 6. 按依赖排序的实现步骤\n\n1. **后端 route 桩**：新建 intent_router.py，挂 `set_current_account` 依赖（§2.4），先返回固定 mock proposal（context_toggle）。include_router 进 main.py。\n2. **iOS 卡片端到端**：建 3 个 Intent 文件；`maybeInterceptAsIntent` 调 route；body 加 sheet/overlay 呈现；`submitTypedQuestion` 抽出 `proceedTypedQuestion` 并前置拦截。打通\"打字→卡片\"，不碰真数据。\n3. **本地预筛 + 降级**：实现 `localPrefilterLooksLikeConfig` + route 短 timeout 降级 + 逃生按钮。验证普通提问零延迟、零误拦死路。\n4. **接 confirm + echo_state（preference 先行）**：`update_coach_preference` 全链路（route→卡片→confirm→`coachPreferenceTextDidChange` 落地）。\n5. **接 toggle_context**：经 `updateContextInclusion` 落地；key 白名单校验在 route。\n6. **接 undo + TOCTOU**：undo 携 `current_app_state` 二次校验；卡片 applied→undone 三态；就地撤销 + 聊天流系统气泡兜底撤销。\n7. **真 LLM 抽槽替换 mock**：单次 LLM 调用替换 step1 桩，复用既有 LLM 客户端，注入 2 类枚举 + 开关白名单 + 当前 app_state 摘要。\n8. （第二批，本期不做）remember_fact / delete_memory(精确 id) / delete_mistake：届时新建 `memory_store.set_status(memory_id, status)` 薄封装（复用 memory_store.py:392 内联 supersede 逻辑），dispatch 调 `record_memory_event`(main.py:5741) / `update_mistake_item`，并为写库类引入后端 undo 快照。\n\n---\n\n## 7. 验收用例（对照路线图二期 AC）\n\n**AC-1 误分流回退为提问（评审 #1/#9 核心）**\n- U1：输入\"二次函数顶点公式是什么？\" → 本地预筛不命中 → 不调 route → 直接 QA 正常作答。**断言：无卡片、无额外 LLM 往返。**\n- U2：输入\"怎么删掉这个分数的公因子？\"（含\"删\"字误命中预筛）→ route 返回 `intent_kind:qa` 或 `needs_clarification` → 不弹删除卡片或弹澄清。**断言：不出现破坏性操作卡片。**\n- U3：任意误弹卡片场景，点\"继续提问\" → `dismissProposal` + 走 `proceedTypedQuestion(原文)` 正常作答。**断言：一键逃生回到 QA，原文未丢。**\n- U4：route 超时(>1.5s)/网络失败 → 降级为普通 QA。**断言：提问不被配置探测拖垮。**\n\n**AC-2 可撤销（评审 #7/#8 核心）**\n- U5：输入\"以后讲题啰嗦一点\" → 卡片 preference，diff 显示前后文本 → 确认 → `coachPreferenceText` 更新 + UserDefaults 持久化 → 卡片 applied 露\"撤销\" → 撤销 → 文本恢复旧值。**断言：UserDefaults `xue.coachPreferenceText` 与 UI 同步回滚。**\n- U6：输入\"把长期记忆关掉\" → 确认走 `updateContextInclusion(\\.memory,false)` → 撤销 → `memory` 回 true。**断言：经唯一写入口，未直接赋值 `contextInclusionSettings`。**\n- U7：确认关记忆后，用户在别处手动又开了记忆，再点撤销 → undo 携 `current_app_state`(memory=true) ≠ 当初改成的值(false) → 后端 `409 state_drifted` → iOS 提示\"设置已变化，未执行撤销\"。**断言：不盲目回写、无静默覆盖。**\n- U8：撤销后卡片为 `undone` 终态，不可再撤。**断言：状态机 pending→confirmed→undone 防重放。**\n\n**AC-3 QA 主流程零改动**\n- U9：语音提问、复习追问、双击画面、quick-followup（7674/8150/8181/8217/8521 路径）全部不触发任何配置拦截，行为与二期前完全一致。**断言：`submitRecognizedQuestion` 调用链未被改动。**\n- U10：旧客户端（不调 /api/intent/*）功能完全无影响。**断言：纯加法、独立端点。**\n\n**AC-4 安全/隔离（评审 #6）**\n- U11：账户 A 拿到的 proposal_id 用账户 B 的 token confirm → 缓存里 account 不匹配 → 拒绝。**断言：per-account 提案隔离。**\n- U12：所有 /api/intent/* 请求进入时 `_current_account` 已 set（依赖注入），无裸端点。**断言：connect() 不落默认库。**\n\n**AC-5 确认前零业务写**\n- U13：route 后不 confirm → 后端无任何业务库变更（首批本就不写库）；提案 10 分钟后从内存缓存过期消失。**断言：pending 不污染任何库、无持久化残留。**\n\n---\n\n## 8. 与原稿差异（修正记录）\n\n- 校正不存在的函数：`record_memory_event` 在 **main.py:5741**（非 8590、非 memory_store）；`memory_store` **无 `set_status`/`record_memory_event`**（仅 392 行内联 supersede + 只读函数）——相关链路全部移入第二批，标注为\"需新建薄封装\"。\n- 校正语音绕过：5 个 submit 点均汇聚 `submitRecognizedQuestion`(8564)，**只改 `submitTypedQuestion` 拦不住语音**；首批显式声明仅文本触发，从而真正不碰 QA 主路径。\n- 砍持久化 `intent_proposals` 表 + 状态机 + `GET /proposals` → 内存 TTL 缓存。\n- 砍 6 类一次性全上 → 首批 2 类（preference/context_toggle）；`add_knowledge` 延后、`prompt_edit` 移出、`match_text` 删除剔除。\n- 砍三层 hybrid 管线 → 单次 LLM 抽槽 + 本地预筛降级税控制。\n- 补：账户 contextvar 写死（§2.4）、undo TOCTOU 二次校验（§4.4）、跨端一致性以本地真源为准（§4.4）、误分类逃生（§5）。\n\n**相关文件（绝对路径）**：\n- iOS：`/home/ubuntu/xue/xue/ios/Xue/App/ContentView.swift`（submitTypedQuestion 8124、submitRecognizedQuestion 8564、composer 731/1685、真源 coachPreferenceText 6207 / updateContextInclusion 6716 / key :32、body 239、网络 11492/11511、开关字段 4197–4260）\n- iOS 新增：`/home/ubuntu/xue/xue/ios/Xue/App/Intent/{IntentModels,IntentProposalCard,IntentRouting}.swift`\n- 后端新增：`/home/ubuntu/xue/xue/backend/app/intent_router.py`\n- 后端复用/对接：`/home/ubuntu/xue/xue/backend/app/main.py`（include_router、set_current_account 561/7867、record_memory_event 5741、upsert_mistake_item 4937）、`/home/ubuntu/xue/xue/backend/app/memory_store.py`（392 supersede、182/398 只读）、`/home/ubuntu/xue/xue/backend/app/prompts.py`（set_prompt 349 等，第二批之外）、`/home/ubuntu/xue/xue/backend/app/db.py`（connect 94、set_current_account 35、_current_account 29）",
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "调研"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "设计"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "对抗评审"
    },
    {
      "type": "workflow_phase",
      "index": 4,
      "title": "汇总规格"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "survey:capabilities",
      "phaseIndex": 1,
      "phaseTitle": "调研",
      "agentId": "a32ed295e8042293a",
      "agentType": "Explore",
      "model": "claude-haiku-4-5-20251001",
      "state": "done",
      "startedAt": 1782401910289,
      "queuedAt": 1782401910285,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/ubuntu/xue/xue/backend/app/db.py",
      "promptPreview": "代码考古。仓库 /home/ubuntu/xue/xue。读 docs/PHASE1_SPEC.md 末尾\"二期NL配置架构要点\"，并核验这些写能力的现有端点与函数(file:line)：\n1) 偏好：一期新增的 coachPreferenceText(ContentView.swift) 是偏好唯一真源、updateContextInclusion(开关唯一写入口)；\n2) 记忆：backend memory_store.py / main.py 的 /api/memory* 写入、agent_memories 表(有无 status active/superseded 软删字段)；\n3) 错题：/api/sessions/{id}/mistakes 写、有无删除/状态端点；\n4) 知识库：/api/knowledge* (reindex/search/新增)；\n5) 提示词：/api/…",
      "lastProgressAt": 1782402008046,
      "tokens": 47140,
      "toolCalls": 32,
      "durationMs": 97756,
      "resultPreview": "完整信息已收集。现在生成报告。\n\n## 核查结果\n\n### 1) 偏好唯一真源与updateContextInclusion\n\n**coachPreferenceText** - /home/ubuntu/xue/xue/ios/Xue/App/ContentView.swift\n- 定义：L6207 `@Published var coachPreferenceText = UserDefaults.standard.string(forKey: coachPreferenceTextDefaultsKey) ?? \"\"`\n- 独立存储key：L32 `private let coachPreferenceTextDefaultsKey = \"xue.coachPreferenceText\"`\n- 写入口：L7913-7915 `func coachPreferenceTextDidCh…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "design:Backend",
      "phaseIndex": 2,
      "phaseTitle": "设计",
      "agentId": "a03db3ed53708fce1",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782402008058,
      "queuedAt": 1782402008056,
      "attempt": 1,
      "promptPreview": "二期目标(见 docs/SMART_AGENT_ROADMAP.md 二期 + PHASE1_SPEC 末尾)：用户用一句自然语言即可配置——改辅导偏好/记住某事/删某条记忆/加知识点/删错题/调上下文开关——通过\"意图识别→Propose 提案(不落库)→用户确认 Confirm→执行(调既有写函数)→可 Undo 撤销\"。后端新建 intent_router.py + intent_proposals 表 + 4端点(POST /api/intent/route|confirm|undo, GET /api/intent/proposals)，复用既有 memory/mistakes/knowledge/prompt/偏好 写函数，QA 主流程零改动。两端共享 AppState。\n代码现状：\n完整信息已收集。现在生成报告。\n\n## 核查结果\n\n### 1) 偏好唯一真源与updateC…",
      "lastProgressAt": 1782402102028,
      "tokens": 11576,
      "toolCalls": 0,
      "durationMs": 93969,
      "resultPreview": "This is a backend architecture spec task — pure design output, no code execution needed. I have all the codebase facts from the context provided. Let me produce the spec.\n\n---\n\n# 二期后端实现规格：意图驱动配置（intent_router）\n\n## 0. 设计总则\n\n- **零侵入 QA**：新增独立路由模块 `intent_router.py` + 4 端点，挂在 `main.py`。QA 主流程 (`/api/sessions/{id}/qa`, L9118) **不改一行**。配置指令与提问完全解耦——客户端决定走哪条路（见 §8）。\n- **确认前不落业务库**：Propose 阶段只写 `intent_p…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "design:iOS-IxD",
      "phaseIndex": 2,
      "phaseTitle": "设计",
      "agentId": "a4d631b9fd98abfc5",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782402008061,
      "queuedAt": 1782402008056,
      "attempt": 1,
      "lastToolName": "Read",
      "lastToolSummary": "/home/ubuntu/xue/xue/ios/Xue/App/ContentView.swift",
      "promptPreview": "二期目标(见 docs/SMART_AGENT_ROADMAP.md 二期 + PHASE1_SPEC 末尾)：用户用一句自然语言即可配置——改辅导偏好/记住某事/删某条记忆/加知识点/删错题/调上下文开关——通过\"意图识别→Propose 提案(不落库)→用户确认 Confirm→执行(调既有写函数)→可 Undo 撤销\"。后端新建 intent_router.py + intent_proposals 表 + 4端点(POST /api/intent/route|confirm|undo, GET /api/intent/proposals)，复用既有 memory/mistakes/knowledge/prompt/偏好 写函数，QA 主流程零改动。两端共享 AppState。\n代码现状：\n完整信息已收集。现在生成报告。\n\n## 核查结果\n\n### 1) 偏好唯一真源与updateC…",
      "lastProgressAt": 1782402151287,
      "tokens": 25651,
      "toolCalls": 17,
      "durationMs": 143226,
      "resultPreview": "I now have a complete picture of the architecture. I have everything needed to produce the design. Let me return the full design as my response.\n\n# App 内\"自然语言配置管家\" — iOS+交互设计\n\n## 0. 核心判断：触发方式\n\n**结论：不加显式入口按钮，不在前端做意图识别。统一走 QA 输入框 → 后端探测意图。** 理由：\n\n1. 用户已经习惯在 composer（追问框/语音）里说话，二期目标就是\"一句话即可配置\"。加按钮等于教用户\"配置要走另一条路\"，违背初衷。\n2. 前端做意图分类要么塞规则（�n脆）要么本地小模型（不现实）。后端 `intent_router.py` 用既有 LLM 能力做意图识别更准、可演进，且 **QA…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "review:adversarial",
      "phaseIndex": 3,
      "phaseTitle": "对抗评审",
      "agentId": "a46fc9500a5c951ae",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782402151293,
      "queuedAt": 1782402151290,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/ubuntu/xue/xue && echo \"=== validate_prompt_conten…",
      "promptPreview": "对抗型评审。狠审二期设计：误分流风险(把提问当配置指令)、安全(未确认就改库/越权改别人数据)、Undo 正确性(逆操作幂等?)、过度设计、是否真零改 QA 主流程、可测性、范围是否过大该切最小可发布。给 必须修正 + 建议砍 两清单。\n\n后端:\nThis is a backend architecture spec task — pure design output, no code execution needed. I have all the codebase facts from the context provided. Let me produce the spec.\n\n---\n\n# 二期后端实现规格：意图驱动配置（intent_router）\n\n## 0. 设计总则\n\n- **零侵入 QA**：新增独立路由模块 `intent_router.py` + 4 端点，挂在 `m…",
      "lastProgressAt": 1782402285440,
      "tokens": 27995,
      "toolCalls": 8,
      "durationMs": 134147,
      "resultPreview": "I have everything needed. The verification confirms several factual errors in both specs plus the design-level problems. Here's the adversarial review.\n\n---\n\n# 对抗型评审：二期\"意图驱动配置\"设计\n\n先说结论：**这版设计建立在一个事实错误的地基上，且范围严重超标。** 核心卖点\"QA 零改动 / 单一漏斗拦截\"在代码里站不住脚。下面按你点的六个攻击面逐一拆，最后给两份清单。\n\n---\n\n## 一、误分流风险（把提问当配置）—— 最严重\n\n**1. \"单一漏斗\"是假的，拦截点选错了。** iOS 设计声称\"所有 typed/voice 文本都汇聚到唯一方法 `submitTypedQuestion`，只在它前面加一个前置 `retu…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "synthesize:spec",
      "phaseIndex": 4,
      "phaseTitle": "汇总规格",
      "agentId": "afb9ec68041f6bf0c",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1782402285445,
      "queuedAt": 1782402285442,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/ubuntu/xue/xue && sed -n '8124,8160p' ios/Xue/App/…",
      "promptPreview": "集成负责人。综合上述+评审，产出**二期实现规格**(可直接照写)：1)后端文件/端点/表(file 级) 2)iOS 改动与组件 3)6类意图 schema 与 Propose/Confirm/Undo 流程 4)数据契约 5)按依赖排序实现步骤 6)验收用例(对照路线图二期AC,尤其误分流回退为提问、可撤销)。最小可发布优先、QA主流程零改动。直接给规格正文。\n\n后端:\nThis is a backend architecture spec task — pure design output, no code execution needed. I have all the codebase facts from the context provided. Let me produce the spec.\n\n---\n\n# 二期后端实现规格：意图驱动配置（intent_router）\n\n…",
      "lastProgressAt": 1782402439104,
      "tokens": 29037,
      "toolCalls": 4,
      "durationMs": 153659,
      "resultPreview": "All facts confirmed. I have what I need to write the integrated, corrected spec. Producing the final spec.\n\n---\n\n# 二期实现规格：自然语言配置管家（intent_router）— 最小可发布版\n\n> 本规格已吸收评审的 9 项必改 + 7 项必砍，并校正了原稿中的事实错误（函数位置/不存在的函数/语音绕过/账户路由层）。所有行号以当前代码为准并已复核。**范围已收敛：首批仅 2 类意图，提案不落业务库、提案态用短 TTL 内存缓存，文本输入框触发，QA 主流程零改动。**\n\n---\n\n## 1. 范围与总则（已收敛）\n\n**首批意图仅 2 类**（评审砍清单 #3）：\n- `update_coach_preference` — 改辅导偏好（真源在 iOS `coachPrefe…"
    }
  ],
  "totalTokens": 141399,
  "totalToolCalls": 61
}